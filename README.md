# Greenland Sea Ice Analysis

Drone-based sea ice floe segmentation and characterization pipeline for visible-light and thermal imagery. Segments individual floes, measures their areas and shapes, and estimates the fractal dimension of floe boundaries as an indicator of ice fragmentation.

---

## Repository Structure

```
greenland_sea_ice_analysis/
├── data/
│   ├── images/
│   │   └── training/         ← {stem}.jpg + {stem}_mask.png pairs for UNet training
│   ├── batch_input/
│   │   └── {flight_name}/    ← images for batch_infer.py (one subfolder per session)
│   └── thermal_tif/          ← raw thermal .tif files for infer_tif.py
├── outputs/                  ← all results (gitignored — generated by scripts)
└── scripts/
    ├── binary_mask.py         ← core segmentation library (required by inference scripts)
    ├── canny_boundary.py      ← Canny edge detection with boundary filtering
    ├── watershed.py           ← watershed-based floe segmentation
    ├── batch_infer.py         ← batch inference on a folder of images
    ├── infer.py               ← single-image UNet inference
    ├── infer_tif.py           ← thermal .tif processing and analysis
    ├── unet_resnet18_infer.py ← ResNet18 UNet inference on visible images
    └── training/
        ├── unet_scratch.py          ← train UNet from scratch (single image)
        ├── unet_multiscale.py       ← train UNet with scale augmentation (multiple images)
        └── unet_resnet18_colab.ipynb ← train ResNet18 UNet on Google Colab (recommended)
```

---

## Installation

```bash
pip install -r requirements.txt
```

For the ResNet18-based model, a GPU is strongly recommended for training (see the Colab notebook). CPU inference is supported for all scripts.

---

## Data Setup

### Placing images for batch inference

Drop images into a subfolder of `data/batch_input/`:

```
data/batch_input/
└── my_flight/
    ├── DJI_001_T.JPG    ← thermal image (auto-detected by pixel count < 1 MP)
    ├── DJI_001_V.JPG    ← visible image (auto-detected by pixel count >= 1 MP)
    └── ...
```

Then set `INPUT_FOLDER = "my_flight"` at the top of `batch_infer.py`.

### Annotating images for training

1. Annotate images in [CVAT](https://cvat.ai) using polygon labels named `ice`
2. Export as **Segmentation mask (PNG)** at the same resolution as the source image
3. Save pairs to `data/images/training/`:
   ```
   data/images/training/
   ├── flight_001.jpg
   ├── flight_001_mask.png
   ├── flight_002.jpg
   ├── flight_002_mask.png
   └── ...
   ```
4. Re-run the training script — pairs are discovered automatically

---

## Scripts

### `scripts/binary_mask.py` — Core Library

Imported by all inference scripts. **Do not run directly.** Key API:

**`FloeSeparator(img_path)`** — load and segment an image
- `preprocess(clahe_clip, clahe_grid, thresh_val)` → `(gray_u8, rgb, mask_u8)`
  Applies CLAHE equalization then binary inverse threshold (255 = ice, 0 = sea).
- `split_erode_dilate_small(mask_u8, min_area)` → `(labels, areas_m, centroids)`
  Erodes large components, runs connected components, dilates each floe back separately.
  Returns a 2D int32 label array (0 = sea, 1..N = floe ID), areas in m², and centroids.

**`characterize_floes(labels)`** → `list[dict]`
Fits an ellipse to each floe contour. Returns per-floe dicts with:
`label`, `area_px`, `perimeter_px`, `semi_major_px`, `semi_minor_px`, `aspect_ratio`, `circularity`

**`compute_fractal_dimension(areas, perimeters)`** → `dict`
Estimates the boundary fractal dimension *D* via OLS log-log perimeter-area regression.
Includes a 95% bootstrap confidence interval. Returns `valid`, `n_floes`, `D`, `r_squared`, `D_ci_low`, `D_ci_high`.

**`measure_raw_contours(mask_u8, min_area_px)`** → `(areas_px, perimeters_px)`
Extracts areas and perimeters from the raw binary mask without morphological smoothing.

---

### `scripts/canny_boundary.py` — Canny Edge Detection

Detects ice floe boundaries using Canny edge detection with Otsu boundary filtering to suppress false edges inside floes.

**Steps:** bilateral smoothing → Canny edges → Otsu ice mask → mask boundary band → keep only edges on boundary → close small gaps.

**Config (top of file):**

| Variable | Default | Description |
|---|---|---|
| `img_path` | `data/images/sea_ice.jpg` | Input image path |
| `LOW_T`, `HIGH_T` | 60, 180 | Canny threshold range — raise both to reduce clutter |

**Run:**
```bash
python scripts/canny_boundary.py
```

**Outputs:** interactive figure (8 subplots showing each step + red-edge overlay). No files saved.

---

### `scripts/watershed.py` — Watershed Segmentation

Segments ice floes using marker-based watershed. Best for images with clear ice-water contrast.

**Config (top of file):**

| Variable | Default | Description |
|---|---|---|
| `FG_FRAC` | `0.35` | Foreground fraction (0.25-0.60) — raise to merge floes, lower to split |
| `USE_ADAPTIVE` | `False` | Use adaptive threshold instead of Otsu (try if low-contrast) |
| `MIN_AREA_PX` | `50` | Minimum component area in pixels |

**Run:**
```bash
python scripts/watershed.py
```

**Outputs** in `outputs/watershed_out/`:
- `01_gray_eq.png` through `09_overlay.png` — step-by-step intermediate images
- `floe_measurements_pixels.csv` — per-floe area and perimeter in pixels

---

### `scripts/batch_infer.py` — Batch Inference Pipeline

Processes all images in `data/batch_input/{INPUT_FOLDER}/`. Auto-detects image type: < 1 MP = thermal, >= 1 MP = visible. Supports both binary mask and UNet segmentation methods.

**Key config variables (top of file):**

| Variable | Default | Description |
|---|---|---|
| `METHOD` | `"binary_mask"` | `"binary_mask"` or `"unet"` |
| `INPUT_FOLDER` | `"thermalimages"` | Subfolder name inside `data/batch_input/` |
| `DRONE_HEIGHT_M` | `92.35` | Flight altitude in metres — **update per flight** |
| `BM_CLAHE_CLIP` | `1.5` | CLAHE clip limit for binary_mask method |
| `BM_THRESH_VAL` | `75` | Binary threshold value (0-255) |
| `FRACTAL_MIN_AREA_M2` | `1.0` | Minimum floe area (m²) for fractal dimension regression |

**Run:**
```bash
python scripts/batch_infer.py
```

**Outputs** per image in `outputs/results/batch/{folder}/{stem}/`:
- `mask.png` — binary ice/sea mask
- `boundary_overlay.png` — original image with yellow floe boundaries
- `floe_size_hist.png` — histogram of floe areas (m²)
- `pa_scaling.png` — log-log P-A plot with fractal dimension *D* (dilated and raw)
- `floe_labels.npy` — 2D int32 label array (0 = sea, 1..N = floe ID)
- `floe_stats.json` — shape metrics per floe + metadata (GSD, fractal dimension)

---

### `scripts/infer.py` — Single-Image UNet Inference

Runs UNet (from scratch) on one image and shows a 3-panel figure (original, mask, boundary overlay). Requires weights from `unet_scratch.py` or `unet_multiscale.py`.

**Key config variables (top of file):**

| Variable | Default | Description |
|---|---|---|
| `MODE` | `"thermal"` | `"rgb"` for visible images, `"thermal"` for thermal |
| `IMAGE_OVERRIDE` | `None` | Override image path, or `None` for mode default |
| `THRESHOLD` | `0.5` | Probability threshold for binary mask |

**Run:**
```bash
python scripts/infer.py
```

**Output** saved to `outputs/results/single/infer_{MODE}_result.png`.

---

### `scripts/infer_tif.py` — Thermal TIF Processing

Processes raw thermal `.tif` files from `data/thermal_tif/`. TIF files must contain float32 temperature values in degrees C (as exported by most drone thermal cameras).

**Key config variables (top of file):**

| Variable | Default | Description |
|---|---|---|
| `INPUT_FILE` | `None` | Specific file stem to process, or `None` for all |
| `ROTATE_180` | `True` | Rotate image 180° before processing |
| `DRONE_HEIGHT_M` | `92.35` | Flight altitude in metres — **update per flight** |
| `BM_THRESH_VAL` | `75` | Binary threshold value |
| `FRACTAL_MIN_AREA_M2` | `1.0` | Minimum floe area (m²) for fractal regression |

**Run:**
```bash
python scripts/infer_tif.py
```

**Outputs** per TIF in `outputs/results/tif/{stem}/`:
- `mask.png`, `boundary_overlay.png` (inferno colormap), `floe_size_hist.png`
- `pa_scaling.png` — log-log P-A plot with fractal dimension *D*
- `floe_labels.npy`, `floe_stats.json`
- `pixel_temp_hist.png` — histogram of all pixel temperatures (degrees C)
- `floe_temp_hist.png` — histogram of per-floe mean temperatures (degrees C)

---

### `scripts/unet_resnet18_infer.py` — ResNet18 UNet Inference

Runs UNet inference using a ResNet18 encoder (via segmentation-models-pytorch) on visible-light images. Requires weights trained with `unet_resnet18_colab.ipynb` placed at `outputs/dataset_tiles_multiscale/best_unet_pretrained.pt`.

**Key config variables (top of file):**

| Variable | Default | Description |
|---|---|---|
| `INPUT_FOLDER` | `"more_visible_test"` | Subfolder inside `data/batch_input/` (must contain `*_V.JPG` files) |
| `WEIGHTS_PATH` | `outputs/.../best_unet_pretrained.pt` | Path to trained weights |
| `DRONE_HEIGHT_M` | `92.35` | Flight altitude in metres |
| `INFERENCE_SCALES` | `[1.0]` | Multi-scale inference — set to `[1.0, 0.5, 0.25]` to enable |
| `THRESHOLD` | `0.5` | Probability threshold |

**Run:**
```bash
python scripts/unet_resnet18_infer.py
```

**Outputs** same structure as `batch_infer.py` — saved to `outputs/results/batch_multiscale/`.

---

## Training

### Which training script should I use?

| Script | When to use |
|---|---|
| `unet_scratch.py` | Single annotated image; quick baseline; CPU training |
| `unet_multiscale.py` | Multiple annotated images; better generalization; CPU training |
| `unet_resnet18_colab.ipynb` | Multiple annotated images; best results; requires GPU (Colab) |

---

### `scripts/training/unet_scratch.py` — Train UNet from Scratch

Tiles a single image+mask pair into 500x500 crops and trains a UNetSmall model (3-level encoder-decoder).

**Key config variables (top of file):**

| Variable | Default | Description |
|---|---|---|
| `IMAGE_PATH` | `data/images/unet_test_ice_2.JPG` | Input image — update to your file |
| `MASK_PATH` | `data/images/unet_test_ice_mask_2.png` | Corresponding mask — update to your file |
| `EPOCHS` | `75` | Training epochs |
| `LR` | `1e-3` | Learning rate |

**Run:**
```bash
python scripts/training/unet_scratch.py
```

**Outputs** in `outputs/dataset_tiles/`:
- `best_unet.pt` — best weights by validation Dice score
- Train/val/test tile folders

Use the weights in `infer.py` by setting `MODE = "rgb"` (or `"thermal"` for a thermal model trained with the thermal variant).

---

### `scripts/training/unet_multiscale.py` — Train UNet with Scale Augmentation

Trains on all `{stem}.jpg` + `{stem}_mask.png` pairs in `data/images/training/` using on-the-fly random 750x750 crops, CLAHE preprocessing, and heavy augmentation (flips, rotations, brightness/contrast jitter, 30% grayscale). Uses a spatial strip train/val split so all conditions appear in both sets.

**Key config variables (top of file):**

| Variable | Default | Description |
|---|---|---|
| `CROP_SIZE` | `750` | Random crop size (pixels) |
| `SAMPLES_PER_EPOCH` | `150` | Random crops drawn per epoch |
| `EPOCHS` | `75` | Training epochs |
| `VAL_FRAC` | `0.30` | Fraction of each image used for validation |
| `USE_CLAHE` | `True` | Apply CLAHE preprocessing (must match at inference time) |

**Run:**
```bash
python scripts/training/unet_multiscale.py
```

**Outputs** in `outputs/dataset_tiles_multiscale/`:
- `best_unet.pt` — best weights by validation Dice score
- `train_curve.png` — loss and Dice curves

Use the weights in `batch_infer.py` by setting `METHOD = "unet"`.

---

### `scripts/training/unet_resnet18_colab.ipynb` — Train ResNet18 UNet (Google Colab)

Trains a UNet with a pretrained ResNet18 encoder (ImageNet weights) using `segmentation-models-pytorch`. The pretrained encoder gives significantly better generalization with limited annotated data.

**Setup:**
1. Open in [Google Colab](https://colab.research.google.com)
2. Go to **Runtime → Change runtime type → T4 GPU**
3. Upload image/mask pairs to Google Drive (any folder)
4. Update `DRIVE_DATA_DIR` and `DRIVE_OUT_DIR` in Cell 3
5. Run all cells top to bottom

**Key config (Cell 3):**

| Variable | Default | Description |
|---|---|---|
| `CROP_SIZE` | `750` | Random crop size |
| `SAMPLES_PER_EPOCH` | `150` | Crops per epoch |
| `EPOCHS` | `75` | Training epochs |
| `LR` | `3e-4` | Learning rate |
| `BATCH_SIZE` | `8` | Batch size (reduce if out of memory) |

**After training:**
Download `best_unet_pretrained.pt` from Drive and place it at:
```
outputs/dataset_tiles_multiscale/best_unet_pretrained.pt
```
Then run `unet_resnet18_infer.py` for inference.

---

## Model Architecture

**UNetSmall** (used by `unet_scratch.py`, `unet_multiscale.py`, `infer.py`)
- 3-level encoder/decoder with skip connections
- ~700K parameters, CPU-friendly
- Loss: BCE + Dice

**UNet + ResNet18** (used by `unet_resnet18_colab.ipynb`, `unet_resnet18_infer.py`)
- Pretrained ResNet18 encoder (ImageNet) + UNet decoder via `segmentation-models-pytorch`
- ~11M parameters, GPU recommended for training

Inference uses a **sliding-window** approach: tiles overlap and predictions are averaged to eliminate seam artifacts.

| Mode | Tile size | Stride |
|---|---|---|
| Visible (scratch) | 500 px | 250 px |
| Thermal | 128 px | 64 px |
| ResNet18 visible | 750 px | 375 px |

---

## Fractal Dimension

Floe boundary complexity is estimated via the perimeter-area (P-A) scaling law:

```
log(P) = (D/2) x log(A) + log(k)   ->   D = 2 x slope
```

- *D* = 1: perfectly smooth (circular) boundaries
- *D* = 2: maximally complex/fractal boundaries
- Typical sea ice: *D* approximately 1.1-1.6

Two estimates are reported per image:
- **dilated** — perimeters after erode/dilate morphological separation (smoothed boundaries)
- **raw** — perimeters measured directly from the binary mask contours

Both include a 95% bootstrap confidence interval and log-log fit R².
Floes smaller than `FRACTAL_MIN_AREA_M2` (default 1.0 m²) are excluded from the regression.

---

## GSD (Ground Sampling Distance)

All measurements are converted from pixels to metres using:

```
GSD (m/px) = DRONE_HEIGHT_M x PIXEL_PITCH_M / FOCAL_LENGTH_M
```

Camera constants (fixed for this drone):
- `PIXEL_PITCH_M = 1.2e-5` m
- `FOCAL_LENGTH_M = 9.1e-3` m

At 92.35 m altitude: GSD approximately 0.1217 m/px.

**Update `DRONE_HEIGHT_M`** at the top of `batch_infer.py`, `infer_tif.py`, or `unet_resnet18_infer.py` for each flight.
